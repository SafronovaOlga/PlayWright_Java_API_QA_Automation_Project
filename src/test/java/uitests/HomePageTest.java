package uitests;import base.BaseTest;import com.microsoft.playwright.Locator;import com.microsoft.playwright.Page;import com.microsoft.playwright.Playwright;import io.qameta.allure.Description;import org.testng.Assert;import org.testng.annotations.Test;import pages.HomePage;import java.util.Collections;import static org.testng.Assert.assertTrue;public class HomePageTest extends BaseTest{        //TC-01 “Menu Fish → Submenus “Saltwater”, “Freshwater”    //    //        Go to https://petstore.octoperf.com/    //        Enter the Store    //        Verify that the left side menu “Fish” has the text “Saltwater”, “Freshwater”        Page page = browser.newPage();    Playwright playwright;   HomePage homePage= new HomePage(page,playwright);            @Test    public void testPageTitle() {        page.navigate(BASE_URL);              Assert.assertEquals(page.title(), "Hands-On Selenium WebDriver with Java");    }        @Test(description = "Verify page title is incorrect")    public void verifyHomePageTitle() {        page.navigate(BASE_URL);               Assert.assertFalse(page.title().contains("Selenium WebDriver2"), "Incorrect title");    }        /*@Test    @Description("Verify the page title on the Selenium WebDriver Java site")    public void verifyHomePageTitle1() {        page.navigate(BASE_URL);        String title = page.title( );                Assert.assertTrue(title.contains("Selenium WebDriver"), "Title does not match!");    }        @Test(description = "Verify 'Getting Started' section is visible")    public void verifyGettingStartedSection() {               homePage.getHomePageUrl();        homePage.scrollTo("Getting Started");            Assert.assertTrue( homePage.isSectionVisible("Getting Started"));        }*/               /*@Test (description = "Verify '<Chapter>' section exists")    public void verifyChapterSection() {        homePage.getHomePageUrl();        homePage.verifyAllChapterLinksAreClickable();               // Assert.assertEquals(homePage.getTitle(), "Loading images");        Assert.assertEquals(("Loading images"),( homePage.clickOnCheckbox( "Chapter 3. WebDriver Fundamentals" ) ) );              //homePage.scrollTo("<Chapter>");       // Assert.assertTrue(homePage.isSectionVisible("<Chapter>"));    }*/        @Test(description = "Check navigation to Web Form")    public void testNavigateToWebForm() {        page.navigate(BASE_URL + "web-form.html");        //  "https://bonigarcia.dev/selenium-webdriver-java/web-form.html"                boolean formPresent = page.isVisible("textarea");                assertTrue(formPresent, "Form textarea should be visible");    }        /*    @Test(description = "Check checkbox interaction")    public void testCheckboxClick() {        page.navigate(BASE_URL + "checkbox.html");        Locator checkbox = page.locator("input[type='checkbox']").first();        checkbox.check();        assertTrue(checkbox.isChecked(), "Checkbox should be checked");    }    */    //  form-group tp-align-right mt-3            }