package uitests;import base.BaseTest;import com.microsoft.playwright.Locator;import com.microsoft.playwright.Page;import org.testng.Assert;import org.testng.annotations.Test;import pages.HomePage;import pages.WebFormPage;import static pages.BasePage.BASE_URL;import static pages.BasePage.WEB_FORM_URL;public class WebFormPageTest extends BaseTest {    Page page;        @Test    void openWebFormTest() {                homePage.openURL();        homePage.linkLocator("Web form").click();                String webFormTitle = webFormPage.getWebFormTitle();                String  actualWebFormUrl = webFormPage.checkIsWebPage();                        Assert.assertEquals(actualWebFormUrl,BASE_URL + WEB_FORM_URL);        Assert.assertEquals(webFormTitle, "Hands-On Selenium WebDriver with Java");                                                                                                                        // webFormPage.openWebFormURL();       // String webFormUrl = webFormPage.getUrl();               // String  title = webFormPage.checkIsWebPage();               // Assert.assertEquals(webFormUrl, BASE_URL + WEB_FORM_URL);       // Assert.assertEquals(title,"Web form");                //assertEquals(BASE_URL + webFormUrl, currentUrl);       // assertEquals("Web form", title.getText());    }    }