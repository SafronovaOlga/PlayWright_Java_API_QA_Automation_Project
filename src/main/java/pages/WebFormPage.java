package pages;import com.microsoft.playwright.Locator;import com.microsoft.playwright.Page;import static pages.BasePage.BASE_URL;public class WebFormPage {        private  Page page;        private static final String WEB_FORM_URL = "web-form.html";    public String pageUrl = "web-form.html";        //Locators   // WebElement submitButton = page.findElement(By.xpath("//button[@type = 'submit']"));        Locator  submitButton;            public WebFormPage( Page page) {                this.page = page;                submitButton = page.locator( "//button[@type = 'submit']");    }        //  @Step("Get subpage url")    public String getUrl() {                return WEB_FORM_URL;    }        public Boolean isCheckedWebFormURL () {               return  page.isChecked(pageUrl);    }        // @Step("Submit form")    public void submitForm() {                submitButton.click();    }        // @Step("Check that page is web form")    public String  checkIsWebPage() {               return BASE_URL + getUrl();       // assertEquals("Web form", getTitle().toString());   //.getText());    }        public String getWebFormTitle() {                return page.title();       // return page.textContent("Web form");    }        }